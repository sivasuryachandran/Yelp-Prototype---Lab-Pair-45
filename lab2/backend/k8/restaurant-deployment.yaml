apiVersion: apps/v1
kind: Deployment
metadata:
  name: restaurant-deployment
  namespace: yelp-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: restaurant
  template:
    metadata:
      labels:
        app: restaurant
    spec:
      initContainers:
        - name: wait-for-mysql
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until nc -z mysql-service 3306; do
                echo "Waiting for MySQL..."
                sleep 2
              done
      containers:
        - name: restaurant
          image: 531396344377.dkr.ecr.us-west-1.amazonaws.com/restaurant-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8003
          envFrom:
            - configMapRef:
                name: yelp-config
            - secretRef:
                name: yelp-secret