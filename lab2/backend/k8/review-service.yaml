apiVersion: v1
kind: Service
metadata:
  name: review-service
  namespace: yelp-app
spec:
  selector:
    app: review
  ports:
    - name: http
      port: 8004
      targetPort: 8004
  type: ClusterIP